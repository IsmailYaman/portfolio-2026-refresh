Work-experience table. One `header` row, then data rows — no card, no shadow, just hairlines.

```jsx
<ExperienceRow header />
<ExperienceRow title="Senior brand designer" company="Jackson Kibby, Berlin" role="Brand designer" year="2023" />
```
