The only field style: no box, no fill — a hairline underline that goes ink-black on focus.

```jsx
<Input label="Name" placeholder="Jane Doe" />
<Input label="Message" multiline rows={5} />
```

Pair with a solid `Button` labelled "Send message". Never add a filled input background.
