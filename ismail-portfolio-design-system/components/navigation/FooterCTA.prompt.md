Closes every page. The headline is the mail link — no form, no button.

```jsx
<FooterCTA headline="Let’s chat" email="ismail@kayadelen.studio" />
```

Keep three columns max, mono links at 14px, and exactly one signal dot.
