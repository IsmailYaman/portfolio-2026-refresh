The only header. Sticky, 72% translucent with a 14px blur, hairline bottom rule.

```jsx
<Navbar name="Ismail Kayadelen" ctaLabel="Contact me" onMenu={() => setOpen(true)} />
```

Use `inverse` over dark hero sections. Navigation links live in `MenuOverlay`, never in the bar.
