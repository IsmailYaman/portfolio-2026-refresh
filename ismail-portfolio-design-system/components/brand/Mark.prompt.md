Placeholder glyph for the lockup — two overlapping dots. Use only inside `Logo`, or as a bullet at 7px.

```jsx
<Mark size={14} color="var(--ev-ink)" />
```

No official logo file exists in the source material; swap this for the real SVG when it arrives.
