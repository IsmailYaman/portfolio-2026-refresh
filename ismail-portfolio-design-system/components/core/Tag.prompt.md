Bracketed mono chip for metadata — years, categories, clients.

```jsx
<Tag>2022</Tag>
<Tag tone="strong" bracket={false}>Branding</Tag>
```

Static only — never clickable, never coloured with an accent.
